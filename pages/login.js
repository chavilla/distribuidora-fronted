import React from 'react';
import Layout from '../components/layout/Layout';

const Login = (props) => {
    return ( 
        <Layout>
            <h1>Desde Login</h1>
        </Layout>
     );
}
 
export default Login;